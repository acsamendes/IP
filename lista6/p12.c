#include <stdio.h>
 
int verificar(int tam, int *v){
    int i;
    for(i=0; i<tam; i++){
        if(tam )
    }
}
 
int main(){
    int len;
    scanf("%d", &len);
    int v[tam], i;
    for(i=0; i<tam; i++) scanf("%d", v+i);
 
    verificar(tam, v);
 
    return 0;
}